package com.example.loto.dto

class TicketSystemCombinationGroup( val howMany: Int = 0,val fromHowMany: Int = 0) : Cloneable {
    private val isSelectedForSystem = false
    private val fixes = 0
    private val payin = 0.0
    private val minPotentialPayout = 0.0
    private val maxPotentialPayout = 0.0
}